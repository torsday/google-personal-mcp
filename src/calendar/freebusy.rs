//! `freebusy` tool group — `query_freebusy` per
//! [ADR-0023](../../docs/adr/0023-calendar-service-surface.md) (with the
//! `calendarExpansionMax: 50` enforcement).
//!
//! Stub: the tool lands in a follow-up ticket (#201). The scaffold reserves the
//! file so the `gmail/` one-file-per-tool-group shape is mirrored.
