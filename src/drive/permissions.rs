//! Drive permissions — `update_permissions` (sharing, behind the literal
//! `yes-share-<file_id>` confirm guard) per
//! [ADR-0025](../../docs/adr/0025-drive-service-surface.md).
//!
//! Stub: the tool implementation lands in #220. This module exists now so the
//! scaffold's module tree matches the final shape and follow-up tickets only add
//! code here rather than restructuring `src/drive/`.
